{% extends 'base.html.twig' %}

{% block title %}
    {{ post.title }}
{% endblock %}

{% block body %}
<h1>{{ post.title }}</h1>
{{ post.body }}
<br><br><a href="{{ path('show_blogs') }}">click here</a> to view all
{% endblock %}